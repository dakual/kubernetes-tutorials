apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-cm
  namespace: default
data:
  value: "Merhaba Dünya!!!"
  path: "/usr/share/nginx/html/index.html"
  config.yaml: |
    namespaces:
      - default
    labels:
      - "app"
      - "owner"  
